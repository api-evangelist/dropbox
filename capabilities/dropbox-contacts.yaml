naftiko: 1.0.0-alpha2
info:
  label: Dropbox API Reference — Contacts
  description: 'Dropbox API Reference — Contacts. 2 operations. Lead operation: Dropbox delete_manual_contacts. Self-contained
    Naftiko capability covering one Dropbox business surface.'
  tags:
  - Dropbox
  - Contacts
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    DROPBOX_API_KEY: DROPBOX_API_KEY
capability:
  consumes:
  - type: http
    namespace: dropbox-contacts
    baseUri: https://api.dropbox.com
    description: Dropbox API Reference — Contacts business capability. Self-contained, no shared references.
    resources:
    - name: 2-contacts-delete_manual_contacts
      path: /2/contacts/delete_manual_contacts
      operations:
      - name: post
        method: POST
        description: Dropbox delete_manual_contacts
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    - name: 2-contacts-delete_manual_contacts_batch
      path: /2/contacts/delete_manual_contacts_batch
      operations:
      - name: post
        method: POST
        description: Dropbox delete_manual_contacts_batch
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: Content-Type
          in: header
          type: string
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    authentication:
      type: bearer
      token: '{{env.DROPBOX_API_KEY}}'
  exposes:
  - type: rest
    namespace: dropbox-contacts-rest
    port: 8080
    description: REST adapter for Dropbox API Reference — Contacts. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/2/contacts/delete-manual-contacts
      name: 2-contacts-delete-manual-contacts
      description: REST surface for 2-contacts-delete_manual_contacts.
      operations:
      - method: POST
        name: post
        description: Dropbox delete_manual_contacts
        call: dropbox-contacts.post
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/2/contacts/delete-manual-contacts-batch
      name: 2-contacts-delete-manual-contacts-batch
      description: REST surface for 2-contacts-delete_manual_contacts_batch.
      operations:
      - method: POST
        name: post
        description: Dropbox delete_manual_contacts_batch
        call: dropbox-contacts.post
        with:
          Content-Type: rest.Content-Type
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: dropbox-contacts-mcp
    port: 9090
    transport: http
    description: MCP adapter for Dropbox API Reference — Contacts. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: dropbox-delete-manual-contacts
      description: Dropbox delete_manual_contacts
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: dropbox-contacts.post
      outputParameters:
      - type: object
        mapping: $.
    - name: dropbox-delete-manual-contacts-batch
      description: Dropbox delete_manual_contacts_batch
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: dropbox-contacts.post
      with:
        Content-Type: tools.Content-Type
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
